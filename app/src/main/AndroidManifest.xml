<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.max.gathernclient.gathernclient">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Filter"></activity>
        <activity android:name=".SearchResult" />
        <activity android:name=".PrivacyPolicy" />
        <activity android:name=".TermsOfUses" />
        <activity android:name=".Faq" />
        <activity android:name=".MyPanorama" />
        <activity android:name=".Reviews" />
        <activity android:name=".HomePage" />
        <activity android:name=".ReservationDetails" />
        <activity android:name=".Congratulations" />
        <activity android:name=".RatingPage" />
        <activity android:name=".TermsAndCancelation" />
        <activity android:name=".LoadPanorama" />
        <activity android:name=".ContactUs" />
        <activity android:name=".Test2" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".PayFortSdkSample" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Start">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Calender" />
        <activity android:name=".AllHouse" />
        <activity android:name=".Parties" />
        <activity android:name=".Offers" />
        <activity android:name=".MyBooking" />
        <activity android:name=".More" />
        <activity android:name=".Search" />
        <activity android:name=".HouseDetails" />
        <activity
            android:name=".TransparentActivity"
            android:label="@string/empty"
            android:theme="@style/Theme.TransNoFloating" />
        <activity android:name=".MyAccount" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Favorite" />
        <activity android:name=".VerificationCode" />
        <activity
            android:name=".SubmitBooking"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".EditMyData" />
        <activity android:name=".HouseImages" />
        <activity
            android:name=".Test"
            android:label="@string/empty"
            android:theme="@style/Theme.TransNoFloating" />
        <activity android:name=".TextInfo" />
        <activity
            android:name=".PaymentPage"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".InfoBookingCOD" />
        <activity android:name=".InfoBookingBankTransefere" />
    </application>

</manifest>